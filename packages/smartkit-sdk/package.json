{
  "name": "@smartkit/sdk",
  "version": "0.1.0",
  "description": "The easiest way to add ERC-4337 smart wallets to your app",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "keywords": ["erc-4337", "account-abstraction", "smart-wallet", "ethereum", "base"],
  "license": "MIT",
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
